"""
server/src/data/ — 数据存储层

这个目录负责"存数据"和"取数据"。
因为现在没有数据库，数据存在 Python 的内存变量里（程序关了数据就没了）。

后续如果要接 MySQL / Redis / 其他数据库，
只需要修改这个目录下的文件，其他代码不用动。

类比：
    前端 Zustand store 的 state —— 存在内存里，页面刷新就没了。
    这里的 data/ 层就是后端的"内存 store"。
"""
